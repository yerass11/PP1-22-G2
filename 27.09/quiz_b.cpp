#include <iostream>
using namespace std;

int main() {
  int n = 3;

  int sum_l = 0;
  int sum_r = 0;

  for(int i = 0; i < n; ++i) {
    int left, right;
    cin >> left >> right;
    sum_l += left;
    sum_r += right;
  }

  // Solve
}